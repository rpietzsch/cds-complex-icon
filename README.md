# Carbon Complex Icon Builder

Small no-build React playground to combine a main Carbon icon with a secondary marker.

## Run

```bash
task run
```

Then open `http://localhost:5173` in your browser.

## Notes

- Runs in the browser with ES modules + import maps.
- Icons are loaded from the Carbon icon library at runtime.
